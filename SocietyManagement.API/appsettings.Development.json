{
  "ConnectionStrings": {
    //"DefaultConnection": "Server=(localdb)\\MSSQLLocalDB;Database=SocietyManagementDb_Dev;Integrated Security=True;TrustServerCertificate=True;MultipleActiveResultSets=true"
    "DefaultConnection": "Server=tcp:mssqlsrv-dev-laundrymgmt.database.windows.net,1433;Initial Catalog=SocietyManagementDb;Persist Security Info=False;User ID=sanjay.shrikant.it.jobs@gmail.com@mssqlsrv-dev-laundrymgmt;Password=Samaira@4818;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;"
  },
  "BlobStorage": {
    // Real value lives in .NET User Secrets (dotnet user-secrets set "BlobStorage:ConnectionString" "...")
    // — never commit the account key here; GitHub's push protection rejects it anyway.
    "ConnectionString": "",
    "ContainerName": "uploads"
  },
  "WhatsApp": {
    // AccessToken/PhoneNumberId live in .NET User Secrets (dotnet user-secrets set "WhatsApp:AccessToken" "...")
    // — never commit the token here; it's a live Meta credential.
    "AccessToken": "",
    "PhoneNumberId": "",
    "ApiVersion": "v25.0",
    "DefaultCountryCode": "91",
    // hello_world is the one template every WABA has pre-approved with zero
    // setup — safe smoke-test default. Swap to your own approved template
    // (with a body {{1}} placeholder) once one exists; see WhatsAppBusinessApiService.
    "TextTemplateName": "hello_world",
    "TextTemplateLanguage": "en_US",
    // festival_payment_success (the real approved template) is body-only, no
    // header — it can't go here (this setting is for a document-HEADER
    // template, which doesn't exist yet, so this stays blank/no-op). It's
    // used instead as FestivalContributionFeature's own dedicated fallback,
    // called directly via SendWhatsAppTemplateAsync — see that file.
    "DocumentTemplateName": "",
    "DocumentTemplateLanguage": "en_US"
  },
  "App": {
    // Local-only: makes the WhatsApp visitor-approval link point back at this
    // dev machine (see launchSettings.json) — a real device can't reach
    // localhost, so this only lets you click the link from the same machine
    // (e.g. copy it from the log/Postman into a browser here) while testing.
    "PublicBaseUrl": "https://localhost:7001"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  }
}
