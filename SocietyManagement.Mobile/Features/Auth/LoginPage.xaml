<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:auth="clr-namespace:SocietyManagement.Mobile.Features.Auth"
             x:Class="SocietyManagement.Mobile.Features.Auth.LoginPage"
             x:DataType="auth:LoginViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceAlt}, Dark={StaticResource OffBlack}}">

    <ScrollView>
        <VerticalStackLayout Padding="32,64,32,32" Spacing="20" MaximumWidthRequest="420" HorizontalOptions="Center">

            <Border BackgroundColor="White" StrokeShape="RoundRectangle 12" Padding="16,12" HorizontalOptions="Center">
                <Image Source="app_logo_full.png" HeightRequest="70" Aspect="AspectFit" />
            </Border>

            <Label Text="Sign in to your account" Style="{StaticResource PageSubtitle}"
                   HorizontalOptions="Center" HorizontalTextAlignment="Center"
                   Margin="0,0,0,4" />

            <Border Style="{StaticResource Card}">
                <VerticalStackLayout Spacing="14">
                    <Label Style="{StaticResource FieldLabel}" Text="Email or Mobile Number" />
                    <Border Style="{StaticResource FieldFrame}">
                        <Entry Text="{Binding Identifier}" Placeholder="e.g. you@example.com or 9XXXXXXXXX" Keyboard="Email" ReturnType="Next" />
                    </Border>

                    <Label Style="{StaticResource FieldLabel}" Text="Password" />
                    <Border Style="{StaticResource FieldFrame}">
                        <Entry Text="{Binding Password}" Placeholder="Password" IsPassword="True" ReturnType="Next" />
                    </Border>

                    <Label Style="{StaticResource FieldLabel}" Text="Society Code" />
                    <Border Style="{StaticResource FieldFrame}">
                        <Entry Text="{Binding SocietyCode}" Placeholder="Society Code" />
                    </Border>
                    <Label Style="{StaticResource Muted}" Text="Not required for Super Admin login." />
                </VerticalStackLayout>
            </Border>

            <Label Style="{StaticResource ErrorText}" Text="{Binding ErrorMessage}"
                   IsVisible="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}}" />

            <Button Text="Sign In"
                    Command="{Binding LoginCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"
                    HeightRequest="48"
                    Margin="0,8,0,0" />

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" HorizontalOptions="Center" />

            <HorizontalStackLayout HorizontalOptions="Center" Spacing="6" Margin="0,16,0,0">
                <Label Text="Need help signing in?" Style="{StaticResource Muted}" VerticalOptions="Center" />
                <Label Text="Contact Us" FontSize="12" TextColor="{StaticResource Primary}" FontAttributes="Bold" VerticalOptions="Center">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnContactUsTapped" />
                    </Label.GestureRecognizers>
                </Label>
            </HorizontalStackLayout>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
