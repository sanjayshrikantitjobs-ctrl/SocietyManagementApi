<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:facilities="clr-namespace:SocietyManagement.Mobile.Features.Facilities"
             xmlns:gen="clr-namespace:SocietyManagement.Mobile.Api.Generated"
             x:Class="SocietyManagement.Mobile.Features.Facilities.MyFacilityBookingsPage"
             x:DataType="facilities:MyFacilityBookingsViewModel"
             x:Name="Root"
             Title="My Bookings">

    <Grid RowDefinitions="Auto,Auto,*">
        <ActivityIndicator Grid.Row="0" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
        <Label Grid.Row="1" Style="{StaticResource ErrorText}" Margin="16,0" Text="{Binding ErrorMessage}"
               IsVisible="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}}" />

        <RefreshView Grid.Row="2" Command="{Binding LoadCommand}" IsRefreshing="{Binding IsBusy}">
            <CollectionView ItemsSource="{Binding Bookings}">
                <CollectionView.EmptyView>
                    <Label Style="{StaticResource Muted}" Text="No bookings yet." HorizontalOptions="Center" Margin="0,24" />
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="gen:FacilityBookingDto">
                        <Border Style="{StaticResource RowCard}" Margin="14,7">
                            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto">
                                <Label Grid.Row="0" Grid.Column="0" Text="{Binding FacilityName}" FontAttributes="Bold" />
                                <Label Grid.Row="1" Grid.Column="0" Style="{StaticResource Muted}">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="{Binding BookingDate, StringFormat='{0:MMM d, yyyy}'}" />
                                            <Span Text=", " />
                                            <Span Text="{Binding StartTime}" />
                                            <Span Text="-" />
                                            <Span Text="{Binding EndTime}" />
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                                <Label Grid.Row="2" Grid.Column="0" Text="{Binding TotalAmount, StringFormat='₹{0:0.00}'}" FontAttributes="Bold" />
                                <Label Grid.Row="0" Grid.Column="1" Text="{Binding Status}" Style="{StaticResource Muted}" />
                                <Button Grid.Row="1" Grid.Column="1" Text="Cancel" FontSize="12" Padding="8,2"
                                        Command="{Binding Source={x:Reference Root}, Path=BindingContext.CancelCommand}" CommandParameter="{Binding .}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>

</ContentPage>
