<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>SocietyManagement.Shared</RootNamespace>
    <AssemblyName>SocietyManagement.Shared</AssemblyName>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <!-- Referenced only so ValidationAppException can wrap FluentValidation.Results.ValidationFailure. -->
    <PackageReference Include="FluentValidation" Version="11.11.0" />
  </ItemGroup>

</Project>
